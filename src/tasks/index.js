const createLibDir = require('./create-lib-dir')
const promptForLibName = require('./prompt-for-lib-name')
const copyTemplates = require('./copy-templates')
const updateLibFiles = require('./update-lib-files')

module.exports = {
  createLibDir,
  promptForLibName,
  copyTemplates,
  updateLibFiles
}